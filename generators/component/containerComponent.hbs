import { connect } from 'react-redux';
import {{ properCase wrappedName }} from '../components/{{ properCase wrappedName }}';

const mapStateToProps = state => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)({{ properCase wrappedName }});
